// Prepare, Plan, Perform, Perfect

// Create an app using Node.js For a comparaison prices sites,

// That's a command line application running once a day
// Scrape a website,
// Check a folder called "data"
    // If the folder doesn't exist, create one
    // else "do nothing"
// Populate the latests price
// Save as a spreadsheet (CSV) 
// Should use npm moudule to assist me in project (content scraper & create csv file)

